# このタスクは THEN 1-1

- **今:** `myukkurivoice-secret` を arm64 に更新する（ブランチ `feature/applecpu/master`）。
- **次（このタスクではない）:** THEN 1-2。`myukkurivoice-vendor` へ評価版 SDK と、更新済み secret の成果物をコピーする。そこで secret を建て直さない。

vendor より先に secret。今は vendor に出さない。
